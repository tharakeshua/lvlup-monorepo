# XPMeter

A horizontal XP progress meter showing the learner's current level and a fill
bar proportional to `xp / next`. Use it in profile headers and gamification
dashboards.

```jsx
<XPMeter level={7} xp={320} next={500} />
```
