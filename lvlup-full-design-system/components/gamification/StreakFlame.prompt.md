# StreakFlame

A compact flame chip showing a learner's current daily streak length. Renders a
lucide `flame` icon and the day count. Use inline in headers or stat rows.

```jsx
<StreakFlame days={14} />
```
