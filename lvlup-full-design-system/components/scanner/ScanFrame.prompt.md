# ScanFrame

Full-bleed camera capture guide with marigold corner guides for aligning an
answer sheet during scanning. Pass `hint` to customize the helper line under the
frame.

```jsx
<ScanFrame hint="Align the answer sheet inside the frame and hold steady" />
```
