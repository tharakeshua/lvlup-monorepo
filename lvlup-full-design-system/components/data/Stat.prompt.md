# Stat

A KPI tile with a label, large value, and an optional delta. Set `trend="up"`
(default) or `trend="down"` to pick the trending icon and color.

```jsx
<Stat label="Active learners" value="1,284" delta="12%" trend="up" />
<Stat label="Churn" value="3.1%" delta="0.4%" trend="down" />
```
