# Skeleton

Loading placeholder. Control `width`/`height`, and use `variant` (e.g. `circle`)
for shape. Defaults to a full-width text line.

```jsx
<Skeleton variant="circle" width={40} height={40} />
<Skeleton width="60%" />
<Skeleton width="100%" height="0.75rem" />
```
