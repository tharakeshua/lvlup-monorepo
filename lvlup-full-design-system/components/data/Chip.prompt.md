# Chip

Filter/tag pill. Set `active` for the selected state and `removable` to show an
"x" remove affordance.

```jsx
<Chip>All</Chip>
<Chip active>Algorithms</Chip>
<Chip removable>System Design</Chip>
```
