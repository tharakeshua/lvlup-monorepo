# AtRiskBadge

A status badge flagging a student's risk level. Defaults to the "At risk"
treatment; pass `level="watch"` for the softer "Watch" variant. Override the
label with `children`.

```jsx
<>
  <AtRiskBadge />
  <AtRiskBadge level="watch" />
</>
```
