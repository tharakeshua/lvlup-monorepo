# Popover

A floating panel surface (`popover` class). Position it relative to a trigger
via the wrapping layout; it renders whatever children you pass.

```jsx
<Popover>
  <DefinitionList items={[{ term: "Status", value: "Active" }]} />
</Popover>
```
