# Tooltip

A small label-on-hover surface (`tooltip` class), rendered as an inline
`<span>`. Pair it with a trigger element in your layout to reveal on
hover/focus.

```jsx
<Tooltip>Server-authoritative grade</Tooltip>
```
