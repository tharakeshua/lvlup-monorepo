# IconButton

Square, icon-only button. Pass the glyph via `icon` (usually an `<Icon>`) and an
accessible `label`. `solid` gives a filled treatment; `size="sm"` shrinks it.

```jsx
<IconButton
  icon={<Icon name="more-horizontal" size={18} />}
  label="More actions"
  solid
/>
```
