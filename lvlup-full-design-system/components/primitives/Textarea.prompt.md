# Textarea

Multi-line text field. Forwards native `<textarea>` props (`rows`, `value`,
`placeholder`, `onChange`, `disabled`). Set `error` for the error border.

```jsx
<Textarea rows={4} placeholder="Explain your reasoning…" error />
```
