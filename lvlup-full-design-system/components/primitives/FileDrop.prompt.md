# FileDrop

Drag-and-drop upload zone. Customize the `title` and `hint`, swap the `icon`
(lucide name), and toggle `active` while a drag is over the target.

```jsx
<FileDrop
  active
  title="Drop your answer sheet"
  hint="PNG or PDF, up to 10MB"
  icon="scan-line"
/>
```
