# Kbd

Inline keyboard-shortcut chip. Wrap one or more keys as `children`. Secondary to
CommandPalette (which renders shortcut hints with the same `kbd` class).

```jsx
<span>
  Press <Kbd>⌘</Kbd> <Kbd>K</Kbd> to open the command palette.
</span>
```
