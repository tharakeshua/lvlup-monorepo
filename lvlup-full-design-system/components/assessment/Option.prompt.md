# Option

A single answer choice for a `QuestionCard`. Use `marker` for the letter/number,
`selected` for the chosen state, and `state="correct"|"incorrect"` to show
grading.

```jsx
<Option marker="A">Array</Option>
<Option marker="B" selected state="correct">Hash map</Option>
```
