# GradePill

A letter-grade chip colored A–F. Pass `grade` for the letter, or add `score` to
render a numeric score styled with the grade's color band.

```jsx
<GradePill grade="A" />
<GradePill grade="C" score="72%" />
```
