// Practice mode — a FLAT single-segment route (direct child of the learn Stack),
// taking spaceId + storyPointId as QUERY params. Same nav-context fix as
// ./content.tsx (deeply-nested routes render outside the navigator on Android).
export { PracticeModeScreen as default } from "../../../lib/screens";
